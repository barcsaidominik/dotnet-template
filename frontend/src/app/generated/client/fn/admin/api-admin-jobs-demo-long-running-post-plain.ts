/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QueuedBackgroundJobResult } from '../../models/queued-background-job-result';

export interface ApiAdminJobsDemoLongRunningPost$Plain$Params {
}

export function apiAdminJobsDemoLongRunningPost$Plain(http: HttpClient, rootUrl: string, params?: ApiAdminJobsDemoLongRunningPost$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<QueuedBackgroundJobResult>> {
  const rb = new RequestBuilder(rootUrl, apiAdminJobsDemoLongRunningPost$Plain.PATH, 'post');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QueuedBackgroundJobResult>;
    })
  );
}

apiAdminJobsDemoLongRunningPost$Plain.PATH = '/api/Admin/jobs/demo-long-running';
