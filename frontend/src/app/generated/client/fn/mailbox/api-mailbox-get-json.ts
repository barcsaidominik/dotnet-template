/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MailboxMessageDto } from '../../models/mailbox-message-dto';

export interface ApiMailboxGet$Json$Params {
}

export function apiMailboxGet$Json(http: HttpClient, rootUrl: string, params?: ApiMailboxGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MailboxMessageDto>>> {
  const rb = new RequestBuilder(rootUrl, apiMailboxGet$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MailboxMessageDto>>;
    })
  );
}

apiMailboxGet$Json.PATH = '/api/Mailbox';
