/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedResultOfProduct } from '../../models/paged-result-of-product';

export interface ApiProductsGet$Plain$Params {
  page?: (number | string);
  pageSize?: (number | string);
  search?: string;
  sortBy?: string;
  sortDescending?: boolean;
}

export function apiProductsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiProductsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResultOfProduct>> {
  const rb = new RequestBuilder(rootUrl, apiProductsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('search', params.search, {});
    rb.query('sortBy', params.sortBy, {});
    rb.query('sortDescending', params.sortDescending, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResultOfProduct>;
    })
  );
}

apiProductsGet$Plain.PATH = '/api/Products';
